# Rojo build artifacts
*.rbxl
*.rbxlx
*.rbxm
*.rbxmx

# ...except source model assets synced in via Rojo $path (not build output)
!src/**/*.rbxm
!src/**/*.rbxmx

# OS / editor cruft
.DS_Store
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# Aftman/Foreman tool installs (if used)
.aftman/
